{
  "compilerOptions": {
    // Need to set these to stop TSC from
    // squigglying modern ES methods +
    // DOM API methods/properties
    "lib": ["ESNext", "DOM"],
    // Disable compiler from outputting files
    "noEmit": true,
    // Appease IDE TS highlighted error:
    // "Cannot use JSX unless the '--jsx' flag is provided."
    "jsx": "react-jsx"
  },
  "include": ["src/**/*", "./globals.d.ts"]
}
